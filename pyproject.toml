[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "misra-checker"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
py-modules = ["misra_checker"]
